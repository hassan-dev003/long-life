/**
 * App shell — replaced with the full top bar / nav / tabs / log / modals in Phase G.
 * Placeholder so the scaffold builds and boots.
 */
export function App() {
  return (
    <div style={{ padding: 32 }}>
      <h1 style={{ letterSpacing: '0.12em', fontWeight: 800 }}>LONG LIFE</h1>
      <p style={{ color: 'var(--text-secondary)' }}>M1 build in progress…</p>
    </div>
  );
}
